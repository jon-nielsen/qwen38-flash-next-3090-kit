# Copy to .env (or export inline) — the composes interpolate these.
# Path to the checkpoint DIRECTORY on the host. BOTH profiles run on halt95's
# downloaded checkpoint AS-IS; surgery/ is optional (series reproducibility).
MODEL=/path/to/checkpoint
VLLM_API_KEY=change-me
