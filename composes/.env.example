# Copy to .env (or export inline) — the composes interpolate these.
# Path to the checkpoint DIRECTORY on the host:
#   Profile A: your surgery output dir (see surgery/RUNBOOK.md)
#   Profile B: halt95's downloaded checkpoint dir, as-is
MODEL=/path/to/checkpoint
VLLM_API_KEY=change-me
